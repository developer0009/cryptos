<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>search</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
        integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <style>
        body {
            background-image: url("https://media.istockphoto.com/photos/virtual-cryptocurrency-bitcoin-crypto-binary-code-symbol-background-picture-id1204582185");
        }
    </style>
</head>

<body style="transform: translateY(23%);">
    <div class="container bg-dark border rounded-lg" style="margin: auto; opacity: 0.9;">
        <form action="/search" id="form" class="text-center pt-3">
            <input type="text" name="crypto_name" placeholder="search crypto">
            <button type="submit" class="btn btn-success  text-white p-1">search</button>
        </form>
        <% for(let coin of coins){%>
            <%if(coin.name.toLowerCase()==value.toLowerCase()){ %>
                <h1 class="text-center text-info pt-3">RESULTS FOUND FOR <%= value %>
                </h1>
                <% count++ %>
                    <br>
                    <br>
                    <img src="<%= coin.iconUrl %> " alt="<%= coin.name %>" class="rounded mx-auto d-block" height=200px"
                        width="200px">
                    <form action="/buycrypto/<%= coin.name %>" method="get" style="float: right;" class="form2">
                        <button type="submit" class=" btn btn-warning text-white rounded-pill" id="buy" name="buy"
                            value="<%= (coin.price)*74.17 %> indian ruppes">buy
                            crypto!</button>
                    </form>
                    <br>
                    <h4 class="text-bold text-white text-center">
                        <marquee>
                            <%= (coin.price)*74.17 %> indian ruppes
                        </marquee>

                    </h4>
                    <% } %>
                        <% } %>
                            <% if(count==0){ %>
                                <h2 class="text-center text-danger ">
                                    try again the value u searched is not found <%=value %>
                                </h2>
                                <% } %>

    </div>

    <script src="/search.js"></script>
</body>

</html>